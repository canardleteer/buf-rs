[package]
name = "xtask"
description = "Workspace maintenance tasks (publish version bumps, etc.)."
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
clap = { version = "4.5", features = ["derive"] }
toml_edit = "0.22"
